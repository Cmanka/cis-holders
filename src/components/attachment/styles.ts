import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    borderWidth: 2,
    borderColor: '#000',
    padding: 20,
  },
  text: {
    textAlign: 'center',
    color: '#000',
  },
});
