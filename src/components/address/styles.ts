import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  text: {
    color: '#000',
  },
  map: {
    height: 200,
    width: '100%',
  },
  toggleWrapper: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  toggleText: {
    color: '#000',
  },
  margin: {
    marginBottom: 15,
  },
});
