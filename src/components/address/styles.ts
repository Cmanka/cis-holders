import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  map: {
    height: 200,
    width: '100%',
  },
  toggleWrapper: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  margin: {
    marginBottom: 15,
  },
});
